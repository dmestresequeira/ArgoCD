deployment:
  enabled: true
  replicas: 2

service:
  enabled: true
  type: NodePort
  spec:
    externalTrafficPolicy: Cluster
  ports:
    web:
      port: 80
      nodePort: 30080
    websecure:
      port: 443
      nodePort: 30443

providers:
  kubernetesCRD:
    enabled: true
  kubernetesIngress:
    enabled: true
    ingressClass: traefik

metrics:
  prometheus:
    enabled: true
    entryPoint: metrics
    addEntryPointsLabels: true
    addServicesLabels: true

ports:
  web:
    port: 80
    protocol: TCP
  websecure:
    port: 443
    protocol: TCP

logs:
  general:
    level: INFO
  access:
    enabled: true
